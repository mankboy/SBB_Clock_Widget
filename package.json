{
  "name": "sbb-clock-widget",
  "version": "0.1.0",
  "description": "SBB Swiss Federal Railways clock widget — standalone desktop app",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-store": "^2"
  }
}
